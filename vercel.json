{
  "buildCommand": "cd artifacts/study-tracker && vite build --config vite.config.ts && mkdir -p ../../dist && cp -r dist/* ../../dist/ || exit 1",
  "installCommand": "pnpm install",
  "nodeVersion": "20.x",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "^/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "^(?!/api/).*",
      "dest": "/index.html"
    }
  ]
}
